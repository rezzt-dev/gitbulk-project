# Gitbulk - Dependencias del proyecto

# --- Dependencias de Ejecución ---
# (El programa base utiliza exclusivamente la librería estándar de Python)

# --- Dependencias de Desarrollo y Compilación ---
# Requerido para generar el ejecutable (.exe / binario)
pyinstaller>=6.0.0